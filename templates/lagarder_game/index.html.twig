{% extends 'base.html.twig' %}

{% block title %}Hello !{% endblock %}

{% block body %}
<div class="container">
    <div class="row ">
        <div class="col-md-6 mt5">
            <table class="table">
                {% for dam in damier  %}
                    <tr>
                        {% for cas in dam %}
                            <td class="{{ cas }}">
                                {{ cas }}
                            </td>
                        {% endfor %}
                    </tr>
                {% endfor %}
            </table>
        </div>
        <div class="col-md-6">

        </div>
    </div>
</div>
{% endblock %}
